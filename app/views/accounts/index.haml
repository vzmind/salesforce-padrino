.block
  .secondary-navigation
    %ul.wat-cf
      %li "oto"
  .content
    %h2.title
      "Accounts"
    .inner
      %table.table
        %tr
          %th.first="ID"
          %th="Name"
          %th.last="&nbsp;"
        -@accounts.each do |account|
          %tr
            %td.first=account["Id"]
            %td=account["Name"]
            %td.last
              ="edit"
              ="|"
              ="delete"
      .actions-bar.wat-cf
        .actions="&nbsp;"

-content_for :sidebar, partial("base/sidebar")
